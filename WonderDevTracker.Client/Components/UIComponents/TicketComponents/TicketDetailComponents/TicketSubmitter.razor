@* TicketSubmitter *@

@inject ITicketDTOService TicketService

<LabeledCard Class="h-100"
			 Title="Submitted By:">
	<ProjectTeamCard User="Ticket.SubmitterUser"
					 PlaceholderName="Unassigned"
					 PlaceholderRole="@Role.Submitter.GetDisplayName()" />
</LabeledCard>

@code {
	[Parameter, EditorRequired]
	public TicketDTO Ticket { get; set; } = default!;
	

}
