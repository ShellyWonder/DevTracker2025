@*ProjectName *@
@* Displays the project's name. Accepts a single parameter, Project. *@

<MudText Typo="Typo.h2" Color="Color.Primary">@Project.Name</MudText>

@code {
    [Parameter, EditorRequired]
    public ProjectDTO Project { get; set; } 

    [Parameter]
    public string? Class { get; set; }
}
